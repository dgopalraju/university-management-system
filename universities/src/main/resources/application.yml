spring:
  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/XEPDB1?currentSchema=university-management-system
    username: university
    password: university123
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format:
          sql: true

server:
  port: 9088
  